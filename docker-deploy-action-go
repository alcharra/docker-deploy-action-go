#!/bin/bash
set -e

BINARY_NAME="deploy-action"

# Build for Windows only (Linux binary should already be committed)
if [[ "$OS" == "Windows_NT" ]]; then
  BINARY_NAME="${BINARY_NAME}.exe"
  go build -o "$BINARY_NAME"
fi

# Run the binary
./"$BINARY_NAME"
